# 이산수학 2장: 논리와 명제 (p.1–26)

> PDF의 강조 표시와 필기 내용을 우선 반영한 시험 대비 요약이다.

## 1. 논리와 명제

### 명제 논리와 술어 논리

- **명제 논리(Propositional Logic)**: 문장 전체를 하나의 변수로 보고, 그 문장의 참·거짓을 판별하는 논리이다.
- **술어 논리(Predicate Logic)**: 문장을 주어(대상·변수)와 술어(관계)로 나누어 참·거짓을 판별한다.

예를 들어 “모든 학생은 시험을 본다”를 나타내면 다음과 같다.

- 명제 논리: `p = "모든 학생은 시험을 본다"`
- 술어 논리: `∀x(학생(x) → 시험본다(x))`
  - `∀`는 all(모든 것), `∃`는 exist(존재)를 뜻한다.

명제 논리는 논리회로와 프로그램 조건문 등에 많이 쓰이고, 술어 논리는 특히 AI, 데이터베이스, 지식 표현 등에 많이 쓰인다.

### 명제(Proposition)

**명제**는 참 또는 거짓을 명확하게 구분할 수 있는 문장이나 식이다. 보통 `p, q, r, ...`로 나타낸다.

- 참이면 `T(true)`
- 거짓이면 `F(false)`
- 명제가 가지는 참·거짓 값을 **진리값(truth value)**이라 한다.
- 진리값이 `T`, `F` 두 가지뿐이므로 **이진 논리**라고 한다.

명제 판별 예시:

| 문장/식 | 명제인가? | 진리값 |
|---|---:|---:|
| 바나나는 맛있다 | 주관적이므로 명제 아님 | - |
| `3x + 5y = 7` | 변수값이 정해지지 않아 명제 아님 | - |
| 28은 4의 배수이다 | 명제 | T |
| 지금 어디로 가는 중입니까? | 의문문이므로 명제 아님 | - |
| `6 < 4` | 명제 | F |
| 유채꽃은 노란색이다 | 명제 | T |
| `3 × 7`의 값은 홀수이다 | 명제 | T |
| 공기는 `H₂O`로 표현된다 | 명제 | F |

## 2. 논리 연산

- **단순 명제(Simple Proposition)**: 하나의 문장이나 식으로 이루어진 명제
- **합성 명제(Compound Proposition)**: 단순 명제들이 논리 연산자로 연결된 명제

| 연산자 이름 | 기호 | 읽는 법/뜻 |
|---|---:|---|
| 부정 | `¬p` 또는 `~p` | not `p`, `p`가 아니다 |
| 논리곱 | `p ∧ q` | `p` and `q` |
| 논리합 | `p ∨ q` | `p` or `q` |
| 배타적 논리합 | `p ⊕ q` | exclusive or, XOR |
| 조건 | `p → q` | if `p`, then `q` |
| 쌍방 조건 | `p ↔ q` | `p` if and only if `q` |

### 2.1 부정(Negation)

`¬p`는 `p`와 반대되는 진리값을 갖는다.

| `p` | `¬p` |
|---:|---:|
| T | F |
| F | T |

분야에 따라 `¬p`, `~p`, `!p`, `p̅` 등의 기호를 사용한다.

### 2.2 논리곱(Conjunction, AND)

`p ∧ q`는 **두 명제가 모두 참일 때만 참**이고, 나머지는 모두 거짓이다.

| `p` | `q` | `p ∧ q` |
|---:|---:|---:|
| T | T | T |
| T | F | F |
| F | T | F |
| F | F | F |

### 2.3 논리합(Disjunction, OR)

`p ∨ q`는 **두 명제가 모두 거짓일 때만 거짓**이고, 하나 이상 참이면 참이다. 여기서 OR는 기본적으로 포함적 OR이다.

| `p` | `q` | `p ∨ q` |
|---:|---:|---:|
| T | T | T |
| T | F | T |
| F | T | T |
| F | F | F |

### 2.4 배타적 논리합(Exclusive OR, XOR)

`p ⊕ q`는 **두 진리값이 서로 다를 때만 참**이다.

| `p` | `q` | `p ⊕ q` |
|---:|---:|---:|
| T | T | F |
| T | F | T |
| F | T | T |
| F | F | F |

### 2.5 조건(Implication)

`p → q`는 “`p`이면 `q`이다”라고 읽는다.

| `p` | `q` | `p → q` |
|---:|---:|---:|
| T | T | T |
| T | F | **F** |
| F | T | T |
| F | F | T |

> **핵심:** 조건명제는 앞의 조건 `p`가 참인데 결과 `q`가 거짓인 경우(`T → F`)에만 거짓이다.

`p → q`의 여러 표현은 모두 같은 뜻이다.

- `p`이면 `q`이다.
- `p`는 `q`의 **충분조건**이다.
- `q`는 `p`의 **필요조건**이다.
- `p`는 `q`를 함축한다.

즉, 화살표 앞 `p`가 충분조건이고 화살표 뒤 `q`가 필요조건이다.

### 2.6 쌍방 조건(Biconditional)

`p ↔ q`는 “`p`이면 `q`이고, `q`이면 `p`이다”라고 읽으며, `p`와 `q`의 진리값이 같을 때 참이다.

| `p` | `q` | `p ↔ q` |
|---:|---:|---:|
| T | T | T |
| T | F | F |
| F | T | F |
| F | F | T |

동치 표현:

- `p` if and only if `q` (`p` iff `q`)
- `p`는 `q`의 필요충분조건이다.

### 2.7 연산자 우선순위

괄호가 없으면 다음 순서로 계산한다.

`not > and > or > if > iff`

즉, `¬ > ∧ > ∨ > → > ↔` 순서이다. 헷갈리면 괄호를 먼저 표시한다.

## 3. 합성 명제 연습

### 문장을 기호로 표현하기

`p`: 비가 온다, `q`: 바람이 분다, `r`: 길이 젖는다.

1. 비가 오거나 바람이 분다: `p ∨ q`
2. 비가 오면 길이 젖는다: `p → r`
3. 비가 오지 않고 바람이 분다: `¬p ∧ q`
4. 비가 오는데 길은 젖지 않는다: `p ∧ ¬r`

### 기호를 문장으로 표현하기

`p`: 날씨가 춥다, `q`: 비가 온다.

1. `¬p`: 날씨가 춥지 않다.
2. `p ∧ q`: 날씨가 춥고 비가 온다.
3. `p ∨ q`: 날씨가 춥거나 비가 온다.
4. `p ∨ ¬q`: 날씨가 춥거나 비가 오지 않는다.

### 진리표 작성 예시

`¬(p ∧ ¬q)`:

| `p` | `q` | `¬q` | `p ∧ ¬q` | `¬(p ∧ ¬q)` |
|---:|---:|---:|---:|---:|
| T | T | F | F | T |
| T | F | T | T | F |
| F | T | F | F | T |
| F | F | T | F | T |

`p ∨ (q ∧ r)`:

| `p` | `q` | `r` | `q ∧ r` | `p ∨ (q ∧ r)` |
|---:|---:|---:|---:|---:|
| T | T | T | T | T |
| T | T | F | F | T |
| T | F | T | F | T |
| T | F | F | F | T |
| F | T | T | T | T |
| F | T | F | F | F |
| F | F | T | F | F |
| F | F | F | F | F |

진리값 예시:

- “`4 + 3 = 7`이고 `4 × 7 = 9`이다”: `T ∧ F = F`
- “유채꽃이 노랗다면 수요일 전날은 토요일이다”: `T → F = F`

## 4. 역, 이, 대우

명제 `p → q`에 대하여:

| 구분 | 식 | 의미 |
|---|---:|---|
| 원명제 | `p → q` | `p`이면 `q`이다 |
| 역(converse) | `q → p` | `q`이면 `p`이다 |
| 이(inverse) | `¬p → ¬q` | `p`가 아니면 `q`가 아니다 |
| 대우(contrapositive) | `¬q → ¬p` | `q`가 아니면 `p`가 아니다 |

중요한 동치 관계:

- 원명제와 대우는 같은 진리값을 갖는다: `p → q ≡ ¬q → ¬p`
- 역과 이는 같은 진리값을 갖는다: `q → p ≡ ¬p → ¬q`
- 일반적으로 원명제와 역은 동치가 아니다.

예: `p → q`가 “날씨가 맑아지면 소풍을 간다”일 때

- 역: 소풍을 가면 날씨가 맑아진다.
- 이: 날씨가 맑아지지 않으면 소풍을 가지 않는다.
- 대우: 소풍을 가지 않으면 날씨가 맑아지지 않는다.

## 5. 항진 명제와 모순 명제

- **항진 명제(Tautology)**: 모든 경우에 항상 참인 합성 명제
- **모순 명제(Contradiction)**: 모든 경우에 항상 거짓인 합성 명제
- 두 종류 모두 아니면 상황에 따라 진리값이 달라지는 명제이다.

| 식 | 분류 | 이유 |
|---|---|---|
| `p ∧ ¬p` | 모순 명제 | 동시에 참일 수 없음 |
| `p ∨ ¬p` | 항진 명제 | 둘 중 하나는 반드시 참 |
| `p ∨ q` | 둘 다 아님 | `p`, `q`에 따라 달라짐 |
| `(p ∧ q) ∧ ¬(p ∨ q)` | 모순 명제 | `p ∧ q`이면 `p ∨ q`도 참이므로 뒤의 부정과 충돌 |

## 6. 논리적 동치 관계

두 명제 `p`, `q`의 쌍방 조건 `p ↔ q`가 항진 명제이면 두 명제는 **논리적 동치(logical equivalence)**이다.

- 표기: `p ≡ q` 또는 `p ⇔ q`
- 뜻: 모든 경우에 두 명제의 진리값이 같다.
- 동치인 한 명제로 다른 명제를 대신할 수 있다.

### 동치임을 증명하는 방법

1. 두 명제의 진리표를 작성하여 모든 행의 진리값이 같은지 확인한다.
2. 한 명제에 논리적 동치의 기본 법칙을 적용하여 다른 명제로 변형한다.

### 반드시 알아둘 기본 법칙

> 주석 포인트: 식뿐 아니라 **법칙의 이름과 내용**을 함께 외워 두는 것이 중요하다. 집합 연산의 법칙과 비슷하게 생각하면 기억하기 쉽다.

| 법칙 | 논리적 동치 |
|---|---|
| 멱등 법칙 | `p ∨ p ≡ p`, `p ∧ p ≡ p` |
| 항등 법칙 | `p ∨ T ≡ T`, `p ∨ F ≡ p`, `p ∧ T ≡ p`, `p ∧ F ≡ F` |
| 부정 법칙 | `¬T ≡ F`, `¬F ≡ T`, `p ∨ ¬p ≡ T`, `p ∧ ¬p ≡ F` |
| 이중 부정 법칙 | `¬¬p ≡ p` |
| 교환 법칙 | `p ∨ q ≡ q ∨ p`, `p ∧ q ≡ q ∧ p`, `p ↔ q ≡ q ↔ p` |
| 결합 법칙 | `(p ∨ q) ∨ r ≡ p ∨ (q ∨ r)`, `(p ∧ q) ∧ r ≡ p ∧ (q ∧ r)` |
| 분배 법칙 | `p ∨ (q ∧ r) ≡ (p ∨ q) ∧ (p ∨ r)`, `p ∧ (q ∨ r) ≡ (p ∧ q) ∨ (p ∧ r)` |
| 흡수 법칙 | `p ∨ (p ∧ q) ≡ p`, `p ∧ (p ∨ q) ≡ p` |
| 드모르간 법칙 | `¬(p ∨ q) ≡ ¬p ∧ ¬q`, `¬(p ∧ q) ≡ ¬p ∨ ¬q` |
| 조건 법칙 | `p → q ≡ ¬p ∨ q` |
| 대우 법칙 | `p → q ≡ ¬q → ¬p` |

### 드모르간 법칙 연습

1. `¬(p ∧ ¬q) ≡ ¬p ∨ q`
2. `¬(¬p ∧ ¬q) ≡ p ∨ q`
3. `¬(¬p ∨ q) ≡ p ∧ ¬q`

괄호 전체를 부정할 때 `∧`와 `∨`가 서로 바뀌고, 괄호 안의 각 명제가 각각 부정된다는 점이 핵심이다.

### 동치 법칙을 이용한 간소화

다음을 간소화하자.

`¬(¬p ∧ q) ∧ (p ∨ q)`

```text
¬(¬p ∧ q) ∧ (p ∨ q)
≡ (¬¬p ∨ ¬q) ∧ (p ∨ q)     드모르간 법칙
≡ (p ∨ ¬q) ∧ (p ∨ q)        이중 부정 법칙
≡ p ∨ (¬q ∧ q)               분배 법칙
≡ p ∨ F                       부정 법칙
≡ p                           항등 법칙
```

## 7. 추론(p.26)

**추론(Inference)**은 주어진 명제 `p₁, p₂, ..., pₙ`이 참이라는 것을 바탕으로 새로운 명제 `q`가 참임을 유도하는 방법이다.

- 주어진 명제 `p₁, p₂, ..., pₙ`: **전제(Premise)**
- 새로 유도한 명제 `q`: **결론(Conclusion)**
- 추론식: `p₁, p₂, ..., pₙ ⊢ q`
  - `⊢`는 “그러므로”, “따라서”라는 뜻이다.

### 유효 추론과 허위 추론

- **유효 추론(Valid Inference)**: 전제가 모두 참이면 결론이 반드시 참인 추론
- **허위 추론(Invalid Inference)**: 전제가 참이어도 결론이 반드시 참이라고 보장할 수 없는 추론

> 추론의 유효성은 전제나 결론이 실제로 참인지와 별개의 문제이다. 핵심은 **전제가 모두 참인 상황에서 결론이 거짓이 될 수 있는가**이다. 그런 반례가 하나라도 있으면 허위 추론이다.

## 시험 직전 핵심 체크

1. 명제는 참·거짓을 **명확히** 판별할 수 있어야 한다.
2. `∧`: 둘 다 참일 때만 참.
3. `∨`: 둘 다 거짓일 때만 거짓.
4. `⊕`: 두 값이 다를 때만 참.
5. `p → q`: **`T → F`일 때만 거짓**.
6. `p ↔ q`: 두 값이 같을 때 참.
7. 충분조건은 화살표 앞, 필요조건은 화살표 뒤이다.
8. 우선순위: `¬ > ∧ > ∨ > → > ↔`.
9. 원명제와 대우가 동치이고, 역과 이가 동치이다.
10. 항진 명제는 항상 참, 모순 명제는 항상 거짓이다.
11. 드모르간 법칙에서는 각 항을 부정하면서 `∧ ↔ ∨`를 바꾼다.
12. 논리적 동치 법칙은 **이름과 식을 함께** 익힌다.
13. 유효 추론은 전제가 모두 참일 때 결론도 반드시 참이어야 한다.
