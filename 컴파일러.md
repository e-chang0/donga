# 컴파일러 강의 요약 필기

> 범위: 1장 객체지향 개요, 2장 C에서 객체지향 언어로
---

# 1장. 객체지향 개요

## 1. 객체지향이 등장한 배경

### 1.1 소프트웨어 위기

1960년대에는 FORTRAN, COBOL과 같은 언어와 어셈블리 언어가 널리 사용되었다. 그러나 프로그램의 규모가 커지면서 개발 비용 증가, 일정 지연, 낮은 품질, 유지보수의 어려움이 나타났고 이를 **소프트웨어 위기(Software Crisis)**라고 부른다.

초기 해결 노력은 다음과 같이 발전했다.

- Algol-68, Lisp 등에서 **블록 구조**가 등장했다.
- 무분별한 `goto` 사용을 줄이고 순차, 반복, 선택으로 프로그램을 구성하는 **구조적 프로그래밍**이 확산되었다.
- Pascal, PL/I, C 등이 1970~1980년대의 대표적인 구조적 언어가 되었다.
- Simula-67은 클래스와 객체 개념을 제공하여 객체지향 언어의 출발점이 되었다.

1968년 NATO 회의에서는 소프트웨어 개발 문제를 공학적으로 다루기 시작했고, 요구 분석부터 유지보수까지 이어지는 **생명주기(Life Cycle)** 관점이 강조되었다.

### 1.2 방법론의 발전

1970년대에는 좋은 프로그램을 만들기 위한 절차와 지침인 **방법론(Methodology)**이 본격적으로 등장했다.

| 방법론/인물 | 핵심 내용 |
|---|---|
| Yourdon, Constantine, DeMarco | 자료 흐름도(DFD), 구조적 분석·설계 등 프로세스 중심 방법 |
| Michael Jackson | JSD(Jackson System Development), 시스템의 자료와 처리 구조를 체계화 |
| Warnier-Orr | 자료 구조와 프로그램 구조를 대응시키는 설계 방법 |

이 시기의 방법론은 주로 **프로세스 중심(Process-Oriented)**이었다. 시스템을 데이터가 흘러가며 처리되는 과정으로 바라보았다.

### 1.3 1980년대의 문제와 객체지향의 필요성

1980년대에는 DBMS, 4세대 언어, CASE 도구 등이 등장했지만 다음 문제는 계속 남았다.

- 전체 소프트웨어 비용의 큰 부분이 유지보수에 사용된다.
- 유지보수에는 기능 개선, 환경 적응, 오류 수정이 모두 포함된다.
- 분석 결과와 구현 코드 사이의 간격이 크다.
- 기존 시스템을 이해하기 위한 역공학(Reverse Engineering)이 어렵다.
- 기능 중심 분해는 데이터와 연산을 분리하여 변경에 취약하다.

객체지향은 현실 세계의 대상과 프로그램의 구조를 비슷하게 만들고, **데이터와 그 데이터를 다루는 연산을 하나의 객체로 묶어** 복잡성과 변경 문제를 줄이려는 접근이다.

---

## 2. 객체지향 언어의 역사

### 2.1 Simula-67

Simula-67은 Ole-Johan Dahl과 Kristen Nygaard가 시뮬레이션을 위해 개발했다. 객체지향의 기초가 되는 다음 개념을 제공했다.

- 클래스와 객체
- 상속
- 가상 메서드(Virtual Method)
- 객체 생성과 메시지에 가까운 호출 구조

**클래스(Class)**는 같은 종류의 객체가 공통으로 가지는 데이터와 연산을 정의한 틀이다. Simula-67의 개념은 이후 Smalltalk, C++, Java 등에 큰 영향을 주었다.

### 2.2 Smalltalk

1970년대 Xerox PARC에서 Alan Kay를 중심으로 개발되었다.

- 거의 모든 것을 객체로 취급하는 순수 객체지향 언어이다.
- 객체 사이의 **메시지 전달(Message Passing)**을 중심으로 동작한다.
- 가상 머신(Virtual Machine) 위에서 실행된다.
- 브라우저 등 통합된 개발 환경을 제공했다.
- 객체지향 프로그래밍과 GUI 발전에 큰 영향을 주었다.

### 2.3 Ada

Ada라는 이름은 Charles Babbage와 함께 일한 Ada Lovelace에서 유래했다. Pascal 계열의 문법을 바탕으로 대규모·신뢰성 높은 시스템을 목표로 개발되었다.

- 예외 처리(Exception Handling)를 제공한다.
- 강한 정적 타입과 정적 바인딩을 중시한다.
- 초기 Ada는 상속을 직접 지원하지 않아 객체 기반(Object-Based) 언어로 분류된다.
- Ada 95에서 객체지향 기능이 강화되었다.

### 2.4 정적 바인딩과 동적 바인딩

**바인딩(Binding)**은 호출할 메서드와 실제 실행 코드를 연결하는 것이다.

| 구분 | 정적 바인딩(Static Binding) | 동적 바인딩(Dynamic Binding) |
|---|---|---|
| 결정 시점 | 컴파일 시점 | 실행 시점 |
| 기준 | 변수의 선언 타입 | 객체의 실제 타입 |
| 장점 | 빠르고 예측하기 쉬움 | 다형성과 확장성이 좋음 |
| 대표 예 | 일반 함수, Ada의 전통적 방식 | Smalltalk, C++ 가상 함수, Java 인스턴스 메서드 |

동적 바인딩은 부모 타입 변수로 서로 다른 자식 객체를 다룰 때 각 객체에 맞는 메서드를 실행하게 해 주며, **다형성(Polymorphism)**의 핵심 기반이다.

### 2.5 C++

AT&T Bell 연구소의 Bjarne Stroustrup이 C를 확장하여 만들었다. 초기 이름은 "C with Classes"였으며 이후 C++로 변경되었다.

- C의 문법, 성능, 포인터를 유지한다.
- 클래스, 상속, 다형성, 가상 함수, 연산자 중복 정의 등을 제공한다.
- 절차적 프로그래밍과 객체지향 프로그래밍을 함께 지원하는 **혼합형(Hybrid) 언어**이다.
- 저수준 제어 능력이 강하지만 메모리 관리와 복잡한 문법은 프로그래머의 책임을 키운다.

### 2.6 Java

1990년대 초 James Gosling을 중심으로 개발되었다. 처음에는 가전제품용 언어인 Oak로 시작했고, 웹의 성장과 함께 Java라는 이름으로 공개되었다.

Java의 설계 방향:

- 단순성
- 객체지향
- 이식성
- 안정성과 보안
- 네트워크·분산 환경 지원

Java는 C++과 비슷한 문법을 사용하지만 포인터 연산, 다중 상속 등 오류를 만들기 쉬운 기능을 줄이고, 가상 머신과 가비지 컬렉션을 채택했다.

### 2.7 그 밖의 객체지향 언어

| 언어 | 특징 |
|---|---|
| Objective-C | C에 Smalltalk식 메시지 전달을 결합. macOS와 iOS 개발에 사용되었고 이후 Swift로 중심이 이동 |
| Eiffel | Bertrand Meyer가 개발. 사전조건·사후조건을 이용한 계약에 의한 설계(Design by Contract)를 강조 |
| Object Pascal | Pascal에 객체지향 기능을 추가 |
| C# | Microsoft의 .NET 플랫폼용 언어. C++·Java와 유사한 문법과 관리형 실행 환경 제공 |
| Loops/Flavors | Lisp 계열에서 객체지향 개념을 실험한 언어·시스템 |

### 언어 발전 흐름

`Simula-67 → Smalltalk → C++/Objective-C/Eiffel/Object Pascal → Java/C#`

- Simula-67: 클래스와 객체 개념의 시작
- Smalltalk: 순수 객체지향과 메시지 전달 확립
- C++: C와 객체지향의 결합
- Java/C#: 가상 머신 또는 관리형 실행 환경을 통한 안전성·이식성 강화

---

## 3. 객체지향 방법론, UML, CASE

### 3.1 대표적인 객체지향 방법론

1990년대에는 객체지향 분석·설계 방법론이 매우 다양했다.

| 방법론 | 핵심 |
|---|---|
| Booch OOD | Grady Booch의 객체지향 설계 방법 |
| Coad & Yourdon OOA | 객체지향 분석 방법 |
| Rumbaugh OMT | Object Modeling Technique. 객체 모델·동적 모델·기능 모델을 사용 |
| Meyer | Eiffel과 계약에 의한 설계 강조 |
| Wirfs-Brock | 책임 주도 설계(Responsibility-Driven Design). 객체의 책임과 협력에 집중 |

객체를 바라보는 대표 관점은 두 가지로 요약할 수 있다.

- **구조적 관점**: 객체가 어떤 속성(Attribute)을 갖고 어떻게 연결되는가?
- **책임 관점**: 객체가 어떤 책임(Responsibility)을 맡고 누구와 협력(Collaboration)하는가?

좋은 설계는 객체의 내부 데이터만 나열하지 않고, 객체가 시스템 안에서 수행해야 하는 책임과 협력까지 함께 고려한다.

### 3.2 UML

방법론마다 서로 다른 표기법을 사용하면서 의사소통 문제가 생겼다. Booch, Rumbaugh, Jacobson은 Rational에서 표기법을 통합하여 **UML(Unified Modeling Language)**을 만들었다.

- 1994년 OOPSLA 전후로 통합 작업이 진행되었다.
- Rumbaugh의 OMT 등 기존 방법론의 표기법을 흡수했다.
- 1998년 OMG(Object Management Group)의 표준이 되었다.
- UML은 개발 방법론 자체가 아니라 시스템을 표현하는 **모델링 언어**이다.
- 분석, 설계, 문서화, 의사소통에 사용된다.

> 암기: UML은 "무엇을 어떤 절차로 개발할지"를 정하는 방법론이 아니라, 시스템의 구조와 행위를 공통 표기법으로 나타내는 언어이다.

### 3.3 CASE

**CASE(Computer-Aided Software Engineering)**는 소프트웨어 분석, 설계, 구현, 유지보수 활동을 도구로 지원하는 것이다.

주요 기능:

- UML 등 모델 작성과 문서화
- 모델의 일관성 검사
- 모델에서 코드 생성: 순공학(Forward Engineering)
- 코드에서 설계 정보 복원: 역공학(Reverse Engineering)
- 메타모델을 이용한 모델 관리

IDE가 주로 코딩·빌드·디버깅을 지원한다면, CASE 도구는 분석·설계 모델과 전체 개발 과정까지 더 넓게 지원한다.

강의의 OODesigner 사례는 OMT/UML 모델을 작성하고 C++ 또는 Java 코드를 생성하는 CASE 도구의 예이다.

---

## 4. 객체지향의 핵심 개념

### 4.1 자료 추상화

**자료 추상화(Data Abstraction)**는 자료와 그 자료에 허용되는 연산을 하나의 논리적 단위로 정의하고, 불필요한 내부 표현은 감추는 것이다.

예를 들어 정수형은 단순한 32비트 저장 공간만을 뜻하지 않는다. 덧셈, 뺄셈, 곱셈, 나눗셈처럼 그 타입에 허용되는 연산까지 함께 의미한다. 클래스는 프로그래머가 새로운 사용자 정의 타입(User-Defined Type)을 만들 수 있게 한다.

좋은 추상화의 효과:

- 사용자는 내부 구현보다 제공되는 연산에 집중한다.
- 구현을 변경해도 공개 인터페이스가 같으면 사용자 코드는 영향을 적게 받는다.
- 잘못된 직접 접근을 막아 객체의 상태를 보호한다.

### 4.2 자료 구조와 컨테이너

Array, Stack, Queue, Linked List, Tree, Graph는 여러 데이터를 조직하고 저장하는 자료 구조이다. 객체지향에서는 이런 구조를 컨테이너 또는 컬렉션 객체로 표현하고, 서로 다른 원소를 공통 방식으로 다루기 위해 다형성을 활용할 수 있다.

### 4.3 객체지향의 대표 특성

| 특성 | 의미 |
|---|---|
| 추상화(Abstraction) | 중요한 특징만 모델링하고 불필요한 세부 사항은 제거 |
| 캡슐화(Encapsulation) | 데이터와 메서드를 한 단위로 묶고 내부 구현을 보호 |
| 정보 은닉(Information Hiding) | 외부에 필요한 인터페이스만 공개하고 구현 세부 사항을 숨김 |
| 상속(Inheritance) | 기존 클래스의 특성을 물려받아 새 클래스를 정의 |
| 다형성(Polymorphism) | 같은 요청이 객체의 실제 타입에 따라 다르게 동작 |
| 동적 바인딩 | 실행 시점에 실제 호출 메서드를 결정 |
| 메시지 전달 | 객체가 다른 객체에 작업을 요청하는 방식 |

### 4.4 객체와 메시지

객체지향 시스템은 객체들이 서로 메시지를 주고받으며 협력하는 구조로 이해할 수 있다.

`수신 객체.메서드(인자)`

예: `stack.push(10)`

- 수신 객체: `stack`
- 메시지 또는 메서드: `push`
- 인자: `10`

절차적 호출 `push(stack, 10)`에서는 자료가 함수의 인자로 전달된다. 객체지향 호출 `stack.push(10)`에서는 연산이 객체에 소속되고, 객체 자신은 암묵적으로 전달된다.

---

## 5. 메모리 할당과 객체 수명

### 5.1 세 가지 메모리 할당 방식

| 구분 | 할당 시점·영역 | 특징 |
|---|---|---|
| 정적 할당 | 프로그램 시작 전 또는 고정된 시점, 정적 영역 | 크기와 수명이 고정적. 전역 변수·정적 변수 등에 사용 |
| 스택 할당 | 함수 호출 시 스택 영역 | 지역 변수와 형식 매개변수 저장. 함수 종료 시 자동 해제 |
| 힙 할당 | 실행 중 필요할 때 힙 영역 | 크기와 수명을 동적으로 결정. 명시적 해제 또는 가비지 컬렉션 필요 |

### 5.2 스택과 힙 비교

| 스택(Stack) | 힙(Heap) |
|---|---|
| LIFO 구조 | 임의 순서로 할당·해제 |
| 함수 호출과 반환에 따라 자동 관리 | 프로그램이 필요할 때 동적으로 사용 |
| 지역 변수, 매개변수 저장 | 동적 객체와 가변 크기 자료 저장 |
| 빠르고 관리가 단순 | 유연하지만 단편화·누수 위험이 있음 |
| 크기가 제한되어 스택 오버플로 가능 | 해제하지 않으면 메모리 누수 가능 |

C의 동적 메모리 예:

```c
int *p = (int *)malloc(100 * sizeof(int));
/* p를 사용 */
free(p);
```

C++에서는 `new/delete`, Java에서는 `new`와 가비지 컬렉션을 사용한다.

### 5.3 C++ 객체와 Java 객체의 차이

```cpp
// C++: 스택에 객체 자체 생성
Person aPerson;

// C++: 힙에 객체 생성
Person *pPerson = new Person();
```

```java
// Java: 변수는 참조, 객체는 힙에 생성
Person aPerson = new Person();
```

Java의 클래스 타입 변수에는 객체 자체가 아니라 **객체를 가리키는 참조**가 저장된다. 반면 `int`, `double`, `boolean` 같은 기본 타입(Primitive Type)은 값을 직접 저장한다.

### 5.4 가비지 컬렉션과 댕글링 참조

**메모리 누수(Memory Leak)**는 더 이상 사용할 수 없는 힙 메모리가 해제되지 않고 남아 있는 현상이다.

**댕글링 참조(Dangling Reference)**는 이미 해제된 메모리를 계속 가리키는 참조이다. C/C++에서는 잘못된 포인터 사용으로 세그멘테이션 오류나 비정상 동작이 발생할 수 있다.

| 방식 | 장점 | 주의점 |
|---|---|---|
| 명시적 해제(C/C++) | 해제 시점을 직접 제어 | 누수, 이중 해제, 댕글링 포인터 위험 |
| 가비지 컬렉션(Smalltalk/Java) | 도달할 수 없는 객체를 자동 회수 | 실행 시간 오버헤드와 회수 시점의 불확실성 |
| 소멸자(C++) | 객체 수명 종료 시 정리 코드 실행 | 포인터·자원 소유 관계를 정확히 설계해야 함 |

> 구분: 메모리 누수는 "필요 없는 메모리가 남는 문제", 댕글링 참조는 "사라진 메모리를 가리키는 문제"이다.

---

## 6. 객체지향 개발의 장점과 주의점

### 장점

- 현실 세계의 개체와 관계를 자연스럽게 모델링할 수 있다.
- 데이터와 연산이 함께 있어 응집도가 높아진다.
- 정보 은닉으로 변경의 영향을 줄일 수 있다.
- 상속과 합성을 통해 기존 코드를 재사용할 수 있다.
- 다형성을 이용해 기존 코드를 크게 바꾸지 않고 새 타입을 추가할 수 있다.
- 분석 모델, 설계 모델, 구현 코드 사이의 대응 관계를 만들기 쉽다.
- 대규모 시스템의 분업과 유지보수에 유리하다.

### 주의점

- 객체지향을 사용한다고 자동으로 좋은 설계가 되는 것은 아니다.
- 지나친 상속, 불필요한 클래스, 복잡한 객체 관계는 오히려 이해를 어렵게 한다.
- 기능을 잘못 사용하거나 언어의 복잡성을 통제하지 못하면 품질이 떨어질 수 있다.
- 작은 프로그램에는 설계 비용이 더 크게 느껴질 수 있다.

핵심은 기능을 무작정 객체로 나누는 것이 아니라 **책임이 명확하고 결합도가 낮은 객체들이 협력하도록 설계하는 것**이다.

---

# 2장. C에서 객체지향 언어로

## 1. C 언어의 특징과 한계

C는 효율이 높고 메모리를 직접 제어할 수 있지만, 그만큼 프로그래머가 책임져야 할 부분이 많다.

### 1.1 포인터

포인터는 변수나 메모리의 주소를 저장한다.

```c
void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

swap(&x, &y);
```

- `&x`: `x`의 주소
- `*a`: `a`가 가리키는 메모리의 값
- 호출자는 주소를 넘기므로 함수가 원래 변수를 변경할 수 있다.

배열과 포인터의 관계:

```c
int getSum(int *a, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += a[i];       // *(a + i)와 같은 의미
    }
    return sum;
}
```

포인터는 강력하지만 주소 계산 오류, 잘못된 형 변환, 배열 범위 초과, 댕글링 포인터 같은 문제를 만들 수 있다.

### 1.2 동적 메모리

```c
int *p = (int *)malloc(sizeof(int));
int *q = (int *)malloc(10 * sizeof(int));
Student *s = (Student *)malloc(sizeof(Student));

free(p);
free(q);
free(s);
```

`malloc()`은 필요한 바이트 수만큼 힙 메모리를 할당하며 초기화와 해제는 프로그래머가 직접 관리한다.

### 1.3 값 전달과 주소 전달

C의 함수 인자는 기본적으로 **값으로 전달(Pass by Value)**된다. 구조체를 값으로 전달하면 구조체 전체가 복사될 수 있다. 원본을 변경하거나 큰 복사를 피하려면 포인터를 전달한다.

```c
void update(Student *s);  // 원본 접근 가능
void print(Student s);    // 값 복사
```

C++의 참조형은 포인터 문법을 단순화한다.

```cpp
void swap(int &a, int &b);
```

### 1.4 C, C++, Java 표현 비교

| 목적 | C | C++ | Java |
|---|---|---|---|
| 동적 배열 | `malloc(n * sizeof(int))` | `new int[n]` | `new int[n]` |
| 동적 객체 | `malloc(sizeof(Student))` | `new Student()` | `new Student()` |
| 멤버 접근 | 구조체 값은 `.`, 포인터는 `->` | 값은 `.`, 포인터는 `->` | 참조 변수에서 `.` |
| 원본 변경 전달 | 포인터 사용 | 포인터 또는 참조 사용 | 객체 참조가 값으로 전달됨 |
| 메모리 해제 | `free()` | `delete`/`delete[]` | 가비지 컬렉션 |
| 가상 메서드 | 없음 | `virtual` | 인스턴스 메서드는 기본적으로 동적 바인딩 대상 |

---

## 2. C의 사용자 정의 타입

구조체를 이용하면 서로 관련된 데이터를 하나의 사용자 정의 타입으로 묶을 수 있다.

```c
typedef struct _student {
    char name[10];
    int kor;
    int math;
    int eng;
    int total;
    double average;
} Student;
```

```c
Student s1;
strcpy(s1.name, "Kim");
s1.kor = 90;
s1.math = 85;
s1.eng = 78;
s1.total = s1.kor + s1.math + s1.eng;
s1.average = s1.total / 3.0;
```

구조체는 자료를 묶어 주지만 C에서는 그 자료를 다루는 함수가 타입 바깥에 존재한다.

```c
void printStudent(Student *s) {
    printf("%s, %d\n", s->name, s->total);
}
```

즉 C의 구조체는 **데이터의 묶음**은 만들지만 데이터와 연산을 하나의 캡슐로 완전히 통합하지는 못한다.

---

## 3. 스택 예제로 보는 자료 추상화의 발전

강의의 핵심 예제는 스택 구현을 단계적으로 개선하여 객체와 클래스가 왜 필요한지를 보여 준다.

### 3.1 1단계: 전역 배열과 전역 변수

```c
int stack[100];
int top = -1;

int main(void) {
    top++;
    stack[top] = 10;

    top++;
    stack[top] = 300;

    int x = stack[top];
    top--;
}
```

문제점:

- 스택 연산이 여러 곳에 흩어진다.
- `top`을 잘못 변경하기 쉽다.
- 구현 방법이 외부 코드에 그대로 노출된다.
- 같은 구조의 스택을 여러 개 만들기 어렵다.

### 3.2 2단계: `push()`와 `pop()` 함수

```c
int stack[100];
int top = -1;

void push(int x) {
    top++;
    stack[top] = x;
}

int pop(void) {
    int x = stack[top];
    top--;
    return x;
}
```

연산에 이름을 붙이고 중복을 줄였지만 함수가 전역 `stack`과 `top`을 직접 사용한다. 따라서 숨은 상태 변경인 **부작용(Side Effect)**이 있으며 여전히 스택 하나만 자연스럽게 처리한다.

### 3.3 3단계: 배열과 `top`을 인자로 전달

```c
void push(int stack[], int *top, int x) {
    *top = *top + 1;
    stack[*top] = x;
}

int pop(int stack[], int *top) {
    int x = stack[*top];
    *top = *top - 1;
    return x;
}
```

여러 스택을 만들 수 있지만 다음 문제가 있다.

- 항상 올바른 배열과 그 배열에 대응하는 `top`을 함께 넘겨야 한다.
- `tower1`과 `top2`처럼 서로 다른 스택의 상태를 잘못 조합할 수 있다.
- 데이터의 논리적 관계를 프로그래머가 수동으로 유지해야 한다.

### 3.4 4단계: 구조체로 관련 상태를 결합

```c
typedef struct _stack {
    int st[100];
    int top;
} Stack;

void initialize(Stack *p) {
    p->top = -1;
}

void push(Stack *p, int x) {
    p->top++;
    p->st[p->top] = x;
}

int pop(Stack *p) {
    int x = p->st[p->top];
    p->top--;
    return x;
}
```

개선점:

- 배열과 `top`이 하나의 `Stack` 값으로 묶인다.
- 여러 스택을 각각 독립적으로 생성할 수 있다.
- 잘못된 배열·`top` 조합 문제를 줄인다.

그러나 외부 코드가 여전히 `p->top`이나 `p->st`에 직접 접근할 수 있으므로 완전한 정보 은닉은 아니다.

### 3.5 5단계: 헤더와 구현 파일 분리

`stack.h`에는 타입과 외부에서 사용할 함수 원형을 둔다.

```c
typedef struct _stack {
    int st[100];
    int top;
} Stack;

void initialize(Stack *p);
void push(Stack *p, int x);
int pop(Stack *p);
```

`stack.c`에는 실제 구현을 둔다.

```c
#include "stack.h"

void initialize(Stack *p) {
    p->top = -1;
}

void push(Stack *p, int x) {
    p->st[++p->top] = x;
}

int pop(Stack *p) {
    return p->st[p->top--];
}
```

`main.c`는 공개된 인터페이스만 사용한다.

```c
#include "stack.h"

int main(void) {
    Stack *p = (Stack *)malloc(sizeof(Stack));
    initialize(p);
    push(p, 10);
    push(p, 300);
    int x = pop(p);
    free(p);
    return 0;
}
```

파일 분리는 구현을 한곳에 모으고 인터페이스를 명확히 한다. 하지만 구조체 정의가 헤더에 공개되어 있다면 멤버를 직접 수정할 수 있으므로 정보 은닉에는 한계가 있다.

### 단계별 핵심 정리

| 단계 | 개선 내용 | 남은 문제 |
|---|---|---|
| 전역 변수 | 가장 단순한 구현 | 상태 노출, 재사용 어려움 |
| 함수화 | 연산을 `push/pop`으로 추상화 | 전역 상태와 부작용 |
| 상태를 인자로 전달 | 여러 스택 사용 가능 | 잘못된 배열·`top` 조합 가능 |
| 구조체 사용 | 관련 데이터를 한 단위로 결합 | 함수가 타입 밖에 있고 멤버가 공개됨 |
| 헤더/구현 분리 | 인터페이스와 구현을 분리 | 언어 차원의 강한 캡슐화 부족 |
| 클래스 사용 | 데이터와 연산 통합, 접근 제어 | 올바른 클래스 설계가 필요 |

---

## 4. C 구조체에서 클래스로

Stroustrup의 초기 목표는 C의 효율을 유지하면서 자료 추상화를 지원하는 것이었다. C의 스택 코드는 다음 변화를 거쳐 클래스가 된다.

### 변화 1: `typedef struct` → `class`

```cpp
class Stack {
    int st[100];
    int top;
};
```

클래스는 데이터 멤버뿐 아니라 그 데이터를 다루는 멤버 함수까지 포함한다.

### 변화 2: 함수가 클래스 내부로 이동

```cpp
class Stack {
    int st[100];
    int top;

public:
    void push(int x);
    int pop();
};
```

기존 C 함수에서 매번 받던 `Stack *p` 매개변수가 사라진다.

```text
initialize(Stack *p)  → initialize()
push(Stack *p, int x) → push(int x)
pop(Stack *p)         → pop()
```

### 변화 3: `p` → `this`

멤버 함수에는 현재 작업 대상 객체를 가리키는 `this`가 암묵적으로 전달된다.

```text
p->top = -1;          → this->top = -1;
p->top++;             → this->top++;
p->st[p->top] = x;    → this->st[this->top] = x;
```

보통 `this->`는 생략할 수 있다.

```cpp
void push(int x) {
    top++;
    st[top] = x;
}
```

### 변화 4: 초기화 함수 → 생성자

```cpp
class Stack {
    int st[100];
    int top;

public:
    Stack() {
        top = -1;
    }

    void push(int x) {
        st[++top] = x;
    }

    int pop() {
        return st[top--];
    }
};
```

**생성자(Constructor)**는 객체가 생성될 때 자동으로 호출되어 객체를 유효한 초기 상태로 만든다.

> 생성자의 핵심 목적은 메모리 할당 자체가 아니라 객체의 상태를 올바르게 초기화하는 것이다.

---

## 5. Java의 클래스와 객체

Java 스택은 C++과 비슷하지만 객체가 힙에 만들어지고 참조를 통해 사용된다.

```java
class Stack {
    private int[] st = new int[100];
    private int top;

    Stack() {
        top = -1;
    }

    void push(int x) {
        st[++top] = x;
    }

    int pop() {
        return st[top--];
    }
}
```

사용 예:

```java
public class Main {
    public static void main(String[] args) {
        Stack stack = new Stack();
        stack.push(10);
        stack.push(300);
        int x = stack.pop();
    }
}
```

### 5.1 객체 생성 과정

`Stack stack = new Stack();`은 다음을 의미한다.

1. `Stack` 타입의 참조 변수 `stack`을 선언한다.
2. `new`가 힙에 `Stack` 객체를 생성한다.
3. 생성자 `Stack()`이 객체를 초기화한다.
4. 생성된 객체의 참조가 `stack`에 저장된다.

### 5.2 C, C++, Java 스택 사용 비교

```c
// C
Stack *p = (Stack *)malloc(sizeof(Stack));
initialize(p);
push(p, 10);
```

```cpp
// C++
Stack *p = new Stack();
p->push(10);
delete p;
```

```java
// Java
Stack p = new Stack();
p.push(10);
```

Java는 객체 메모리를 가비지 컬렉터가 관리하므로 `delete`를 직접 호출하지 않는다.

### 5.3 메시지 전달로 이해하기

```java
stack.push(10);
```

이는 "`stack` 객체에게 `10`을 저장하라는 `push` 메시지를 보낸다"고 해석할 수 있다.

```text
push(stack, 10)  →  stack.push(10)
```

왼쪽은 함수 중심 표현이고, 오른쪽은 수신 객체 중심 표현이다. Smalltalk은 이 메시지 전달 관점을 특히 강하게 강조한다.

### 5.4 `this`와 메서드 바인딩

```java
void eat(Hamburger x) {
    rightHand.grab(x);
    rightHand.move(mouth.getLocation());
    rightHand.putTo(x, mouth);
    weight = weight + x.weight();
}
```

`kilDong.eat(aHamburger)`를 호출하면 메서드 내부의 `this`는 `kilDong`에 바인딩된다. `youngHee.eat(aHamburger)`를 호출하면 같은 코드 안의 `this`가 `youngHee`에 바인딩된다.

즉 메서드 코드는 하나여도 **어느 객체를 수신자로 호출했는지**에 따라 접근하는 객체 상태가 달라진다.

### 5.5 정적 메서드

`static` 메서드는 특정 객체가 아니라 클래스에 소속된다. 따라서 인스턴스 수신 객체와 `this`가 없다.

```java
double y = Math.sin(x);
```

여기서 `sin()`은 `Math` 클래스의 정적 메서드이다. `main()`도 객체 생성 전 실행 진입점이 되어야 하므로 정적 메서드이다.

| 인스턴스 메서드 | 정적 메서드 |
|---|---|
| 객체에 소속 | 클래스에 소속 |
| `this` 사용 가능 | `this` 없음 |
| 객체 상태에 접근 가능 | 인스턴스 멤버에 직접 접근 불가 |
| `obj.method()` | `ClassName.method()` |

---

## 6. C++와 Java 비교

| 항목 | C++ | Java |
|---|---|---|
| 실행 방식 | 주로 기계어로 컴파일 | 바이트코드와 JVM 사용 |
| 객체 위치 | 스택 또는 힙에 생성 가능 | 객체는 기본적으로 힙에 생성 |
| 객체 변수 | 객체 자체 또는 포인터 | 객체 참조 |
| 메모리 관리 | `new/delete`, RAII, 스마트 포인터 | 가비지 컬렉션 |
| 포인터 연산 | 가능 | 일반 응용 코드에서 제공하지 않음 |
| 다중 상속 | 클래스 다중 상속 가능 | 클래스 단일 상속, 인터페이스 다중 구현 |
| 전역 함수 | 가능 | 함수는 클래스 내부에 정의 |
| 연산자 중복 정의 | 지원 | 일반적인 사용자 정의 연산자 중복 정의 미지원 |
| 배열 | 크기 정보가 별도로 필요할 수 있음 | 배열 객체가 `length` 보유 |
| 문자열 | 라이브러리 타입과 문자 배열 사용 | `String` 객체 사용 |
| `union` | 지원 | 직접적인 `union` 없음 |

Java가 C/C++에서 단순화하거나 제거한 대표 요소:

- 포인터 산술
- 수동 메모리 해제
- 전처리기 중심 구조
- 클래스의 다중 상속
- 전역 함수와 전역 변수
- `union`

대신 Java는 배열 범위 검사, 가비지 컬렉션, 표준화된 실행 환경을 제공하여 안전성과 이식성을 높인다.

---

# 시험 직전 핵심 정리

## 반드시 구분할 개념

1. **클래스 vs 객체**
   - 클래스: 데이터와 연산을 정의한 타입 또는 설계도
   - 객체: 클래스에 따라 실제로 생성된 실행 시점의 실체

2. **캡슐화 vs 정보 은닉**
   - 캡슐화: 데이터와 메서드를 한 단위로 묶는 것
   - 정보 은닉: 외부에서 구현 세부 사항에 접근하지 못하게 하는 설계 원리

3. **상속 vs 다형성**
   - 상속: 기존 클래스의 특성을 물려받는 타입 정의 방법
   - 다형성: 같은 요청이 실제 객체 타입에 따라 다르게 동작하는 성질

4. **정적 바인딩 vs 동적 바인딩**
   - 정적: 컴파일 시 호출 대상 결정
   - 동적: 실행 시 실제 객체 타입에 따라 호출 대상 결정

5. **스택 메모리 vs 힙 메모리**
   - 스택: 호출 구조에 따라 자동 관리
   - 힙: 실행 중 동적으로 할당하며 별도의 수명 관리 필요

6. **메모리 누수 vs 댕글링 참조**
   - 누수: 쓸 수 없는 메모리가 회수되지 않음
   - 댕글링 참조: 이미 해제된 메모리를 가리킴

7. **인스턴스 메서드 vs 정적 메서드**
   - 인스턴스: 객체와 `this`가 필요
   - 정적: 클래스에 속하며 `this`가 없음

## 서술형 포인트

### 객체지향이 등장한 이유

대규모 소프트웨어의 복잡성과 유지보수 비용이 증가하면서 데이터와 함수를 분리한 기존 방식의 한계가 나타났다. 객체지향은 관련 데이터와 연산을 객체로 묶고 정보 은닉, 상속, 다형성을 제공하여 변경 영향을 줄이고 재사용성과 확장성을 높이기 위해 등장했다.

### 스택 예제가 보여 주는 것

전역 배열과 `top`에서 시작해 함수화, 상태 인자 전달, 구조체, 헤더·구현 분리, 클래스로 발전한다. 이 과정은 단순한 코드 문법 변화가 아니라 **관련 상태의 결합 → 연산의 추상화 → 인터페이스와 구현의 분리 → 데이터와 연산의 캡슐화** 과정을 보여 준다.

### `this`가 필요한 이유

하나의 메서드 코드를 여러 객체가 공유하므로, 현재 호출의 대상이 어느 객체인지 구분해야 한다. `this`는 현재 메시지를 받은 객체를 가리키며 메서드가 그 객체의 인스턴스 변수에 접근할 수 있게 한다.

### UML과 CASE의 차이

UML은 시스템을 시각적으로 표현하기 위한 표준 모델링 언어이다. CASE는 분석, 설계, 코드 생성, 역공학 등 소프트웨어 공학 활동을 자동화·지원하는 도구 또는 환경이다. CASE 도구가 UML을 사용할 수 있지만 둘은 같은 개념이 아니다.

## 한 줄 암기

- Simula-67: 클래스와 객체 개념의 시작
- Smalltalk: 순수 객체지향과 메시지 전달
- C++: C + 객체지향, "C with Classes"
- Java: C++ 문법을 단순화하고 JVM·가비지 컬렉션 채택
- UML: 표준 모델링 언어
- CASE: 소프트웨어 공학 활동을 지원하는 도구
- 자료 추상화: 자료와 허용 연산을 하나의 타입으로 정의
- 객체 호출: `수신 객체.메서드(인자)`
- 생성자: 객체를 유효한 초기 상태로 만듦
- 동적 바인딩: 실행 시 실제 객체 타입에 맞는 메서드 선택
