# 알고리즘 강의 필기 (2026년 2학기, 1~2주차)

## 1주차: 알고리즘과 주요 설계 기법

### 1. 알고리즘의 뜻과 성능

**알고리즘**은 입력값(또는 입력값들의 집합)을 받아 출력값을 만드는 **잘 정의된 계산 절차**다. 예를 들어 정렬 알고리즘은 `<31, 41, 59, 26, 41, 58>`을 받아 오름차순으로 재배열한 `<26, 31, 41, 41, 58, 59>`를 출력한다. 출력은 원래 원소를 그대로 포함하면서 `a₁ ≤ a₂ ≤ ⋯ ≤ aₙ`을 만족해야 한다.

효율성은 실행 속도, 메모리, 병렬 처리 적합성, 전력 소모 등에 따라 달라진다. 이 강의에서 가장 기본적으로 보는 척도는 **시간복잡도**다. 시간복잡도는 입력 크기 `n`에 따라 필요한 연산 횟수가 어떻게 증가하는지를 나타내며, **점근적 복잡도**는 특히 `n`이 충분히 클 때의 증가율을 본다. `O` 표기는 증가율의 상한을 표현한다. 최악의 경우와 평균적인 경우를 자주 분석한다.

증가율의 차이는 입력이 커질수록 두드러진다. 예를 들어 `n²`은 다항식 시간, `2ⁿ`은 지수 시간이며 충분히 큰 `n`에서는 `2ⁿ`이 훨씬 빠르게 증가한다.

### 2. 어려운 문제와 근사 알고리즘

| 분류 | 핵심 의미 |
|---|---|
| `P` | 답을 다항 시간에 **찾을** 수 있는 결정 문제 |
| `NP` | 주어진 답이 맞는지 다항 시간에 **검증**할 수 있는 결정 문제 |
| `NP-완비` | `NP`에 속하고, `NP`의 모든 문제만큼 어려운 문제 |

- 여행하는 외판원 문제의 결정형: 모든 도시를 한 번씩 방문하고 출발점으로 돌아오는 비용 `K` 이하의 경로가 있는가?
- 그래프 `k`-색칠 문제: 이웃한 정점들이 다른 색을 갖도록 `k`개 색으로 칠할 수 있는가? 일반 그래프에서 `k ≥ 3`이면 NP-완비다. 모든 **평면 그래프**는 4색으로 칠할 수 있다는 사실과 구분한다.
- NP-완비 문제 중 하나라도 다항 시간에 풀 수 있음이 증명되면 모든 NP 문제를 다항 시간에 풀 수 있다.
- **근사 알고리즘**은 최적해를 구하기 어려울 때 다항 시간에 최적해에 가까운 해를 구한다. 강의자료는 근사해 값과 최적해 값의 비율을 근사비로 소개하며, 최적해의 2배 이내를 보장하는 예를 `2`-근사라고 부른다. 최소화·최대화 문제에 따라 보장 비율을 해석하는 방향은 달라진다.

### 3. 정렬과 분할 정복

| 정렬 | 작동 원리 | 평균 시간 | 최악 시간 |
|---|---|---:|---:|
| 버블 정렬 | 이웃한 두 원소를 비교하고 필요하면 교환 | `O(n²)` | `O(n²)` |
| 선택 정렬 | 남은 원소 중 최솟값을 골라 앞으로 이동 | `O(n²)` | `O(n²)` |
| 삽입 정렬 | 앞쪽의 정렬된 구간에 다음 원소를 끼워 넣음 | `O(n²)` | `O(n²)` |
| 퀵 정렬 | 피벗을 기준으로 양쪽을 분할 | `O(n log n)` | `O(n²)` |
| 병합 정렬 | 반으로 나눠 정렬한 뒤 병합 | `O(n log n)` | `O(n log n)` |

퀵 정렬은 피벗이 계속 최솟값 또는 최댓값으로 골라져 분할이 한쪽으로 치우치면 최악 `O(n²)`이 된다.

**분할 정복**은 (1) 같은 형태의 작은 문제로 **분할**, (2) 부분 문제를 재귀적으로 **정복**, (3) 부분 해를 **결합**하는 방법이다. 병합 정렬이 대표적이다. `n × n` 행렬 두 개의 단순 곱셈은 `O(n³)`이고, 강의자료에 소개된 기본적인 블록 분할 방식도 전체 연산량은 `O(n³)`이다. 다만 블록 계산은 병렬 처리에 적합하다.

### 4. 동적 계획법과 최적 부분 구조

**동적 계획법(DP)**은 반복해서 나타나는 부분 문제를 **한 번만 풀고 결과를 저장·재사용**한다. 피보나치 수열에서 `F(1)=F(2)=1`, `F(n)=F(n-1)+F(n-2)`다. 단순 재귀는 같은 값을 여러 번 계산하므로 지수적으로 많은 호출을 만들지만, 저장하면 `O(n)` 시간에 계산할 수 있다.

**최적의 원칙**은 전체 문제의 최적해가 적절한 부분 문제들의 최적해로 이루어진다는 뜻이다. 예를 들어 `u`에서 `v`까지의 최단 경로가 `w`를 지난다면, 그 경로의 `u → w`, `w → v` 구간도 각각 해당 구간의 최단 경로여야 한다.

### 5. 그래프·그리디·해시

- **그래프** `G=(V,E)`: `V`는 정점 집합, `E`는 간선 집합이다. 방향/무방향 그래프가 있으며 인접 행렬 또는 인접 리스트로 표현할 수 있다.
- **플로우 네트워크**: 간선마다 용량이 있고, 중간 정점에서는 들어오는 유량과 나가는 유량이 같다. 최대 플로우 문제는 출발점 `s`에서 도착점 `t`까지 보낼 수 있는 최대 유량을 찾는다. 네트워크·자원 배분·영상 분할 등에 쓰인다.
- **그리디 알고리즘**: 매 단계에서 지금 가장 좋아 보이는 선택을 한다. 모든 문제에서 최적해를 보장하지는 않으므로 해당 문제에서 그리디 선택이 옳음을 따로 확인해야 한다.
- **허프만 코딩**: 그리디 알고리즘의 예. 빈도 높은 문자에는 짧은 코드, 빈도 낮은 문자에는 긴 코드를 준다. 빈도가 가장 낮은 두 항목을 반복해서 묶어 트리를 만든다.
- **해시 테이블**: 키를 해시 함수에 넣어 저장 주소를 정한다. 서로 다른 원소가 같은 주소로 가는 **충돌**을 해결해야 한다.

### 6. 행렬·머신러닝·검색 트리

- **선형방정식의 행렬 표현**: `Ax=b`. 정방행렬 `A`가 역행렬을 가지면 `x=A⁻¹b`가 유일한 해다. 방정식 수가 미지수보다 적으면 해가 여러 개일 수 있고, 더 많으면 정확한 해가 없을 수 있어 최소제곱 근사를 쓴다.
- **최소제곱 근사**: 데이터와 모델 사이 오차의 제곱합을 최소화한다. 양·음 오차의 상쇄를 막고 큰 오차에 더 큰 비용을 준다.
- **클러스터링**: 유사도에 따라 `n`개의 데이터 점을 `k`개의 그룹으로 나눈다. 강의자료에서는 각 점을 가장 가까운 중심의 클러스터에 할당하는 그림을 제시한다.
- **경사하강법**: 함수값이 낮아지는 방향으로 값을 갱신한다. 볼록하지 않은 함수에서는 일반적으로 지역 최소점으로 수렴할 수 있다.
- **이진 검색 트리(BST)**: 각 노드의 왼쪽에는 더 작은 키, 오른쪽에는 더 큰 키가 있다. 검색·삽입·삭제의 시간은 트리 높이 `h`에 비례하는 `O(h)`다. 삽입 순서에 따라 트리 높이가 달라진다.
- BST 삭제: 삭제 노드가 잎이면 바로 제거한다. 자식이 하나면 그 자식으로 자리를 잇는다. 자식이 둘이면 보통 **직후 원소(중위 후속자)**를 찾아 삭제 노드 자리로 옮기고, 후속자가 원래 있던 자리를 정리한다.

## 2주차: 점근적 표기법과 정렬 시간복잡도

### 1. 분석의 전제

점근적 표기는 입력 크기 `n`이 충분히 커질 때 함수의 **증가율**을 비교한다. 상수배와 낮은 차수의 항은 큰 `n`에서 주도적인 증가율을 바꾸지 않는다. 강의에서는 단일 프로세서의 RAM 모델을 가정해 명령을 순서대로 실행하고, 실제 CPU·메모리 환경에서 측정한 초 단위 시간 대신 연산 횟수의 증가율을 비교한다.

아래 정의에서는 `f(n)`과 `g(n)`을 충분히 큰 `n`에서 음수가 아닌 함수로 생각한다. 엄밀히는 `f ∈ O(g)`처럼 **집합 소속**으로 써야 하지만 관례적으로 `f(n)=O(g(n))`처럼 적는다.

### 2. 세 가지 기본 표기

| 표기 | 의미 | 정의: 어떤 양의 상수와 `n₀`가 존재하여 모든 `n ≥ n₀`에 대해 |
|---|---|---|
| `f(n)=O(g(n))` | 점근적 **상한** | `f(n) ≤ c·g(n)` |
| `f(n)=Ω(g(n))` | 점근적 **하한** | `c·g(n) ≤ f(n)` |
| `f(n)=Θ(g(n))` | 점근적으로 **같은 차수** | `c₁·g(n) ≤ f(n) ≤ c₂·g(n)` |

`O`와 `Ω`에서 `c > 0`, `Θ`에서 `c₁,c₂ > 0`이다. `n₀` 이후의 **모든** 입력 크기에서 부등식이 성립해야 한다. 작은 `n`에서의 예외는 허용된다.

**핵심 정리:** `f(n)=Θ(g(n))`일 필요충분조건은 `f(n)=O(g(n))`이고 동시에 `f(n)=Ω(g(n))`인 것이다. 두 조건의 시작점이 `n₁`, `n₂`로 다르면 `n₀=max(n₁,n₂)`로 잡으면 된다.

예를 들어 `5n²+3=Θ(n²)`임을 직접 보이려면 `n≥2`에서 `n² ≤ 5n²+3 ≤ 6n²`이므로 `c₁=1`, `c₂=6`, `n₀=2`를 선택하면 된다. 강의자료에는 `5n²`와 `n²/2-5`도 `Θ(n²)`인 예로 나온다. `n²/2-5`의 하한은 충분히 큰 `n`부터 잡아야 한다.

> `O(n²)`는 “꼭 이차 시간”이라는 뜻이 아니다. `n`, `n log n`, `n²`처럼 `n²`보다 빠르게 증가하지 않는 함수가 모두 들어간다. 정확한 차수를 말할 때는 `Θ`를 쓴다.

### 3. 엄격한 상·하한: 작은 `o`, 작은 `ω`

- `f(n)=o(g(n))`: `f`가 `g`보다 **엄격히 느리게** 증가한다. 양의 함수에 대해 `limₙ→∞ f(n)/g(n)=0`이면 성립한다.
- `f(n)=ω(g(n))`: `f`가 `g`보다 **엄격히 빠르게** 증가한다. 양의 함수에 대해 `limₙ→∞ f(n)/g(n)=∞`이면 성립한다.
- 예: `n log n=o(n²)`, `n³=ω(n²)`. 반면 `5n²=Θ(n²)`이므로 `o(n²)`도 `ω(n²)`도 아니다.

`n²`를 기준으로 관계를 한 줄로 정리하면 다음과 같다.

| `f(n)` 예 | `o(n²)` | `O(n²)` | `Θ(n²)` | `Ω(n²)` | `ω(n²)` |
|---|---:|---:|---:|---:|---:|
| `n` | ✓ | ✓ |  |  |  |
| `5n²+3` |  | ✓ | ✓ | ✓ |  |
| `n³` |  |  |  | ✓ | ✓ |

### 4. 삽입 정렬: 입력 배열에 따라 달라지는 시간

삽입 정렬은 왼쪽의 정렬된 부분에 다음 원소 `key`를 끼워 넣는다. 앞의 원소가 `key`보다 크면 한 칸씩 오른쪽으로 밀고, 빈 자리에 `key`를 넣는다. 강의자료 예시: `5 2 4 6 1 3 → 2 5 4 6 1 3 → 2 4 5 6 1 3 → ⋯ → 1 2 3 4 5 6`.

```text
for i = 1 .. n-1:
    key = A[i]
    j = i-1
    while j >= 0 and A[j] > key:
        A[j+1] = A[j]
        j = j-1
    A[j+1] = key
```

| 경우 | 입력 상태 | 전체 시간 |
|---|---|---:|
| 최선 | 이미 오름차순: 매 단계에서 안쪽 반복이 바로 끝남 | `Θ(n)` |
| 평균 | 삽입 위치가 대략 중간이라고 보는 강의의 설명 | `Θ(n²)` |
| 최악 | 역순: `i`번째 원소를 앞쪽 끝까지 옮김 | `Θ(n²)` |

최악에는 이동·비교 횟수의 주요 합이 `1+2+⋯+(n-1)=n(n-1)/2=Θ(n²)`이다. 평균도 대략 그 절반이므로 차수는 `Θ(n²)`다. 이미 정렬된 배열에서는 바깥 반복 `n-1`회만 필요하므로 `Θ(n)`이다.

**표현 주의:** “삽입 정렬의 시간복잡도는 `Θ(n²)`”라고 조건 없이 말하면 최선의 경우가 빠진다. “최악 시간 `Θ(n²)`”, “평균 시간 `Θ(n²)`”, “최선 시간 `Θ(n)`”처럼 경우를 밝힌다. 모든 입력에 대한 상한으로 `O(n²)`, 하한으로 `Ω(n)`이라고 말할 수 있다.

### 5. 버블 정렬: 강의자료의 기본 구현 기준

버블 정렬은 인접한 원소를 비교해 순서가 틀렸으면 교환한다. 한 번 순회할 때 큰 값이 뒤로 이동하고, 정렬되지 않은 범위를 줄여 반복한다. 강의자료가 분석한 **기본 구현**은 조기 종료를 하지 않으므로 비교 횟수가 입력 순서와 무관하게 `1+2+⋯+(n-1)=n(n-1)/2`다.

| 경우 | 기본 버블 정렬의 시간 |
|---|---:|
| 최선 | `Θ(n²)` |
| 평균 | `Θ(n²)` |
| 최악 | `Θ(n²)` |

따라서 이 구현에는 조건 없이 `Θ(n²)`라고 해도 된다. 이미 정렬됐는지 확인해 **교환이 없는 순회에서 중지하는 최적화**를 추가한 버전이라면 최선 시간은 `Θ(n)`으로 달라지지만, 이는 강의자료의 비교 횟수 분석과 다른 구현이다.

### 2주차 핵심 확인

1. `O`는 상한, `Ω`는 하한, `Θ`는 두 방향의 상·하한을 모두 만족하는 같은 차수다.
2. 점근적 정의를 증명할 때는 적절한 양의 상수 `c`(또는 `c₁,c₂`)와 시작점 `n₀`를 **직접 제시**한다.
3. 작은 `o`와 `ω`는 각각 엄격히 느림과 엄격히 빠름을 뜻하며, 같은 차수인 `Θ`와 구별한다.
4. 삽입 정렬은 최선 `Θ(n)`, 평균·최악 `Θ(n²)`이다.
5. 강의자료의 기본 버블 정렬은 최선·평균·최악 모두 `Θ(n²)`이다.
